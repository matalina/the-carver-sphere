---
layout: base.njk
title: Dungeon Master Hideout
description: A warlock
---

<h1>{{ title }}</h1>

<div class="md: flex md: flex-wrap">
  <div>
  {% set top = null %}
  {%- for item in collections.abcDm -%}
    {% set current = item.data.title.slice(0,1).toUpperCase() %}
    {% if current !== top %}
      {% set top = current %}
      </div>
      <div class="md:w-1/3 mb-3 p-3">
      <h2 class="pb-3 mb-5 text-center border-b border-purple-700 text-purple-700">{{ top }}</h2>
    {% endif %}
    <a href="{{ item.data.url }}">{{ item.data.title }}</a><br/>
  {%- endfor -%}
  </div>
</div